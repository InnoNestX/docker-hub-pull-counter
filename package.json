{
  "name": "docker-hub-pull-counter",
  "version": "1.0.0",
  "description": "Query total Docker Hub pull counts for a user",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "keywords": [
    "docker",
    "dockerhub",
    "pull-count",
    "api"
  ],
  "license": "MIT",
  "dependencies": {
    "@upstash/redis": "^1.28.0",
    "@vercel/speed-insights": "^2.0.0",
    "hono": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
